<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/postagens">Blog</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/postagens">Postagens</a>
                    </li>
                    {{#if usuario}}
                    <li class="nav-item">
                        <a class="nav-link" href="/postagens/nova">Nova Postagem</a>
                    </li>
                    {{/if}}
                </ul>
                <ul class="navbar-nav">
                    {{#if usuario}}
                    <li class="nav-item">
                        <span class="navbar-text me-3">Olá, {{usuario.nome_completo}}</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Sair</a>
                    </li>
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Entrar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/cadastro">Cadastre-se</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        {{#if mensagem}}
        <div class="alert alert-{{#if (eq mensagem.tipo 'erro')}}danger{{else}}success{{/if}} alert-dismissible fade show" role="alert">
            {{mensagem.texto}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/if}}

        {{{body}}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>